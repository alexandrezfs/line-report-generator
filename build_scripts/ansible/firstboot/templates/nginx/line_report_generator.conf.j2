upstream backend {
    {% set number = number_of_process | int %}
    {% for count in range(0, number) %}
       server localhost:50{{ "%02d" | format(count) }} fail_timeout=5 max_fails=3;
    {% endfor %}
}

server {
    listen 80;

    location / {
        proxy_pass http://backend;
    }
}
